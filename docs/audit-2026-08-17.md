# Program-Manager Audit — 2026-08-17 (healthy run; registry write blocked)

STATUS: COMPLETE read-only audit. `.hermes/PROGRAM.md` + `.hermes/*` writes are BLOCKED in
this cron sandbox (protected files, no approver present). No registry mutation was possible
and none was attempted via workarounds. Full delta below for the next writable run.

## Drift inventory (verified against repo)

### STALE ROWS in PROGRAM.md
1. "Free-money hub + calculator" lists `src/components/FreeMoneyCalculator.tsx` —
   file does NOT exist. Row should be hub-only.
2. Rows for Unclaimed / Bank bonuses / Free stock / Build credit list
   `src/app/free-money/{unclaimed,bank-bonuses,free-stock,credit}/page.tsx` —
   none of these files exist. `public/_redirects` shows deliberate consolidation:
   all four 301 → `/find`. Only `/free-money` and `/free-money/class-actions` routes exist.

### SEO — NO regression, do NOT restore removed routes
- sitemap.xml: `/free-money`, `/free-money/class-actions`, `/cart`, `/file/*` present ✓
- llms.txt: `/free-money`, `/free-money/class-actions`, `/cart`, `/find` present ✓
- Removed sub-routes are 301'd to `/find` by design — adding them back to sitemap/llms.txt
  would put redirect/404 URLs in the index. The AI-SEO cron is preserving the vertical correctly.

### NEW files (last ~12h)
- `scripts/_cron_bucket_check.py` — longtail-coverage helper (needs row).
- `scripts/.fbm_seen_state.json` — fbm runtime state (note only).

### OVERLAP / ORPHANS
- fbm_* family: `fbm_pipeline.py`, `fbm_alert_engine.py`, `fbm_cdp_feed.py`,
  `fbm_playwright_feed.py` = experimental siblings of canonical `scripts/fbm_scraper.py`
  (only one cron-wired, via `~/.hermes/scripts/freebie_ingest.py`, 30m). Flag, don't merge.
- `functions/api/checkout.ts` — NO caller anywhere in src/ (superseded by cart-checkout +
  billing/portal + webhooks/stripe). Orphaned endpoint; flag, don't delete.
- Dev probes not cron-wired: `_probe.py`, `_supabase_probe.py`, `_verify_live_count.py`,
  `_junk_softhide.py`. Harmless.
- No orphaned data files: free-money.ts, classActions.ts, grants.ts, interestTaxonomy,
  off-market-funders.json all imported. `/free-money` linked in nav + sitemap + llms.txt.

### Live issues (not repo drift, watch)
- Class-action 5am cron errored 08-16 05:00 ("ABORT: only 1 settlements scraped") — false
  abort from count-parser bug, ALREADY PATCHED in `~/.hermes/scripts/refresh-class-actions.sh`
  (comment documents it); next run 08-17 05:00 validates.
- freebie-alert ingest cron failing 2 consecutive runs (DDG/FB 202 bot checks, 0 listings).

## RECONCILIATION DELTA for PROGRAM.md (apply at next writable run)
1. Hub row: files = `src/app/free-money/page.tsx`, `src/components/FreeMoneyLeadCapture.tsx`.
2. Replace 4 sub-page rows with one "removed vertical (301 → /find)" entry.
3. Add rows: DFY filing engine (src/lib/dfy.ts, src/lib/cart.ts, src/app/cart/,
   src/app/file/[slug]/ + ClaimWizard, functions/api/{cart-checkout,dfy-confirm,dfy-wizard,
   grant-applications,billing/portal,webhooks/stripe}); Lead capture + trust events
   (functions/api/leads, FreeMoneyLeadCapture, migration 005); FB Marketplace freebie
   pipeline (fbm_scraper.py canonical + siblings flagged + .fbm_seen_state.json); Longtail
   research + ingest (docs/research/longtail-*.txt, ingest_longtail.py, _cron_bucket_check.py);
   Aggregators API (functions/api/aggregators/* — owner unrecorded, flag).
4. Merge-log entries dated 2026-08-17 covering all of the above.
5. "Known duplication risks": add fbm_* siblings + checkout.ts orphan.
6. `.hermes/.audit-probe.md` (33-byte probe from degraded 08-16 run) — harmless, may delete.

No files were deleted or modified. This note is the only artifact of this run.
